{
  "name": "blackhole-audio-visualizer",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@blackhole/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "electron:dev": "cd apps/desktop && bun run dev",
    "electron:package": "cd apps/web && bun run build:electron && cd ../../apps/desktop && bun run package",
    "electron:package:mac": "cd apps/web && bun run build:electron && cd ../../apps/desktop && bun run package:mac",
    "electron:package:win": "cd apps/web && bun run build:electron && cd ../../apps/desktop && bun run package:win",
    "test": "turbo test",
    "test:coverage": "turbo test -- --coverage",
    "prepare": "husky"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "husky": "^9.1.7",
    "prettier": "^3.7.4",
    "turbo": "^2",
    "vitest": "^4.0.18"
  }
}
